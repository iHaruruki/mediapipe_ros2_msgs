```yaml
std_msgs/Header header   # 同期元(color)の時刻/座標系
string name              # ランドマーク名（nose, left_eye_inner, ...）
uint16 index             # ランドマークid
float32 x                # 画像ピクセルx（フル画像座標）
float32 y                # 画像ピクセルy（フル画像座標）
```